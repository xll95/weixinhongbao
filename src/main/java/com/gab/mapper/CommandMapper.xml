<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace 指向当前的Mapper接口 -->
<mapper namespace="com.gab.mapper.CommandMapper">
	<select id="findAllCommand" resultType="command">SELECT * FROM command WHERE  STATUS=1 ORDER BY sort DESC </select>
</mapper>